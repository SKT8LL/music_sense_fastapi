# 데이터베이스 설정 (Azure Database for PostgreSQL 예시)
# 형식: postgresql://{user}:{password}@{host}:{port}/{database}?sslmode=require
DATABASE_URL=postgresql://myadmin:mypassword@my-azure-db.postgres.database.azure.com:5432/music_sense?sslmode=require

# 애플리케이션 환경 (dev, test, prod)
ENV=prod

# 모델 경로 (Azure App Service는 주로 /home/site/wwwroot 또는 엄격한 상대 경로를 사용합니다)
# 컨테이너 배포의 경우, 이 경로는 Dockerfile의 COPY 대상에 따라 달라집니다.
MODEL_PATH=/app/models/model.onnx

# Azure Web App은 일반적으로 포트 80/443을 컨테이너로 전달합니다.
# Uvicorn은 0.0.0.0:$PORT 또는 8000에서 수신 대기해야 합니다.
# 시작 명령어를 사용하는 경우: uvicorn main:app --host 0.0.0.0 --port 8000
